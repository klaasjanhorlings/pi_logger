module.exports = {
    paths: {
        source: {
            server: './source/server'
        },
        config: {
            tsServer: './source/server/tsconfig.json'
        },
        dest: {
            build: './build'
        }
    }
}